import React from "react";
const ButtonSecondDsa = ({ text }: { text: string }) => {
  return (
    <button className=" px-8 font-bold py-3 signInbut text-white rounded-xl text-lg">
      {text}
    </button>
  );
};

export default ButtonSecondDsa;
